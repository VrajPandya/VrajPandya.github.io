source "https://rubygems.org"

gem "jekyll", "~> 4.2"
gem "webrick", "~> 1.8"

# The macOS system Ruby in this workspace is 2.6.x. Keep ffi on a compatible
# line so local `bundle install` does not resolve to a Ruby 3-only platform gem.
gem "ffi", "~> 1.16.3"
